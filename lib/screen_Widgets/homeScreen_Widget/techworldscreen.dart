import 'package:flutter/material.dart';

class TechWorldScreen extends StatelessWidget {
  const TechWorldScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(
        Icons.computer,
        size: 60,
      ),
    );
  }
}
